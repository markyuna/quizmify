/** Default per-question time limit for "against the clock" mode. */
export const TIMED_MODE_SECONDS_PER_QUESTION = 20;

/** Sentinel `userAnswer` sent to /api/checkAnswer when a question times out. */
export const TIMEOUT_ANSWER_SENTINEL = "__TIMEOUT__";
